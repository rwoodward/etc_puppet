# Default limit for number of user's processes to prevent
# accidental fork bombs.
# See rhbz #432903 for reasoning.

*          soft    nproc     20000
root       soft    nproc     unlimited

#ulimit config for mapr installation
mapr - nproc 64000
